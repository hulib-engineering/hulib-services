name: Pull Request

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  checks:
    name: Lint / Typecheck / Unit tests
    uses: ./.github/workflows/ci.yml